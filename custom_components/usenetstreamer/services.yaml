apply_config:
  name: Apply config
  description: Apply UsenetStreamer configuration values through the admin API.
  fields:
    entry_id:
      name: Config entry id
      description: The UsenetStreamer config entry ID to update.
      required: true
      selector:
        text:
    values:
      name: Config values
      description: Mapping of UsenetStreamer config keys and values to save.
      required: true
      selector:
        object:
